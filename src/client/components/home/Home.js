import React from "react";
import { gql, useQuery } from "@apollo/client";

function Home() {
  return (
    <div>
      <input type="text" />
      <button>Login</button>
    </div>
  );
}

export default Home;
